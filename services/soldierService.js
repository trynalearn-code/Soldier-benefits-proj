import soldiersRepo from "./repositories/xrepo";

async function createSoldier(soldierData){
    if (!soldier.name) throw new Error("Missing name")
    // const newSoldier =
    // const result = await xrepo.createSoldier(soldier)
    soldier.tostring
}

async function gets(){
    return await soldiersRepo.gets()
}